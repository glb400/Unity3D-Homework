using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DiskAttribute : MonoBehaviour
{
    // Attributes of disks (color + size + speed)
    // Aiming at create disks with random attribute
    // preset enum for colors
    public Color color;
    public float size;
    public float speed;
}
